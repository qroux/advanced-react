import { Typography } from '@material-ui/core';

export const Home = () => {
  return (
    <div>
      <Typography variant='h4' gutterBottom>
        Welcome to homepage
      </Typography>
    </div>
  );
};
