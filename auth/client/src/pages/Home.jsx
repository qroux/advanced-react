import { Typography } from '@material-ui/core';

export const Home = () => {
  return (
    <div>
      <Typography variant='h4' gutterBottom style={{ textAlign: 'center' }}>
        Welcome to homepage
      </Typography>
    </div>
  );
};
