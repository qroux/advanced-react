import { Typography } from '@material-ui/core';

export const Home = () => {
  return (
    <Typography
      variant='h4'
      gutterBottom
      style={{ textAlign: 'center' }}
      color='textPrimary'>
      Welcome to homepage
    </Typography>
  );
};
